name: Create a -built branch

on:
  push:
    branches:
      - main
      - develop
      - front-end

jobs:
  built-branch:
    uses: alleyinteractive/.github/.github/workflows/built-branch.yml@main
