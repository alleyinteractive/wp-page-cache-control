name: Built Release

on:
  push:
    branches:
      - develop
      - main
      - production

jobs:
  built-release:
    uses: alleyinteractive/.github/.github/workflows/built-release.yml@feature/deprecate-workflows
    with:
      node: 20
